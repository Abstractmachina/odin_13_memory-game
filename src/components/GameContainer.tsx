import React, {FC, ReactElement} from 'react';

const GameContainer: FC = (): ReactElement => {
    return (
        <div className='gameContainer'>ffffff</div>
    );
}

export default GameContainer;